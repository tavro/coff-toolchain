# fibonacci test
# expected result: 55 (fib(10) = 55)

fib: (p0) {
    if (p0 <= 1) {
        return p0
    };
    x0 = fib(p0 - 1);
    x1 = fib(p0 - 2);
    return x0 + x1
}

main: () {
    return fib(10)
}
